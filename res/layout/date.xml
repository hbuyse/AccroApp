<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/largeurDateMois"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="@dimen/paddingAdapter"
    android:id="@+id/date_mois" >
    
	<TextView
	    android:id="@+id/mois"
	    android:layout_width="fill_parent"
	    android:layout_height="wrap_content"
	    android:layout_marginTop="@dimen/margeDateHaut"
	    android:background="@color/color_mois"
	    android:gravity="center"
	    android:text="@string/size_jour"
	    android:textColor="@color/color_jour"
	    android:textSize="@dimen/fontJour"
	    android:textStyle="bold" />

	<TextView
	    android:id="@+id/date"
	    android:layout_width="fill_parent"
	    android:layout_height="wrap_content"
	    android:layout_alignParentRight="true"
	    android:layout_below="@+id/mois"
	    android:background="@color/color_jour"
	    android:gravity="center"
	    android:text="@string/size_mois"
	    android:textColor="@color/color_mois"
	    android:textSize="@dimen/fontMois"
	    android:textStyle="bold" />

</RelativeLayout> 